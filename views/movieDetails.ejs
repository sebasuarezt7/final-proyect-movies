<%- include('partials/header') %>

<main>
  <div class="movie-details">
    
    <h1><%= movie.name %></h1>

    <p><strong>Description:</strong> <%= movie.description %></p>
    <p><strong>Year:</strong> <%= movie.year %></p>
    <p><strong>Genres:</strong> <%= movie.genres.join(", ") %></p>
    <p><strong>Rating:</strong> <%= movie.rating %>/10</p>

    <hr>

    <% if (currentUser && currentUser._id == movie.createdBy.toString()) { %>
      <div class="movie-actions">
        
        <a href="/movies/edit/<%= movie._id %>" class="btn btn-primary">
          Edit Movie
        </a>

        <form action="/movies/delete/<%= movie._id %>" method="POST">
          <button class="btn btn-danger">Delete Movie</button>
        </form>

      </div>
    <% } %>

      <a href="/movies" class="back-link">← Back to Movies</a>

  </div>
</main>

<%- include('partials/footer') %>